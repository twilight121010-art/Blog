# 全栈学习计划

## 适用范围

这份计划不是泛泛而谈的全栈路线，而是围绕当前这个项目来学：

- 前端：`public/index.html`、`public/app.js`、`public/styles.css`
- 后端：`server.js`
- 数据：`data/store.json`
- 部署：Node.js + 宝塔 + Nginx 反向代理

目标不是先学一堆名词，而是边改项目边学，最后能自己维护、扩展、部署这个网站。

## 当前项目你已经接触到的全栈内容

- 页面渲染与交互
- 表单处理
- `fetch` 请求
- 登录态保存
- Node.js HTTP 服务
- 接口设计
- 文件存储
- 密码哈希
- 服务器部署
- 域名访问

这已经是一个完整的小型全栈项目了，只是现在的数据层还比较简单。

## 学习顺序

建议按这个顺序学，不要反过来：

1. 前端基础
2. 当前项目前端代码结构
3. 后端 HTTP 与接口
4. 当前项目后端代码结构
5. 数据存储与登录态
6. Linux / 宝塔 / Nginx / 部署
7. 数据库
8. 云存储
9. 安全与正式上线优化

## 8 周实战计划

## 第 1 周：把前端基础补起来

学习重点：

- HTML 表单、输入框、按钮、`label`
- CSS 布局：`flex`、`grid`、响应式
- JavaScript 基础：变量、函数、数组、对象、条件、事件
- DOM 操作：`querySelector`、`addEventListener`

在项目里重点看：

- `public/index.html`
- `public/styles.css`
- `public/app.js`

本周目标：

- 看懂登录页和注册页是怎么渲染出来的
- 看懂按钮点击、输入变化、失焦校验这些逻辑
- 能自己改文案、颜色、边距、按钮样式

建议你自己动手改 3 个小点：

- 改一处按钮文案
- 改一个模块的颜色或圆角
- 改一个表单字段的提示语

## 第 2 周：把当前前端逻辑彻底看懂

学习重点：

- 状态管理的基本思路
- 前端如何组织一个稍复杂页面
- 页面模块切换
- 前端如何发请求给后端

在项目里重点看：

- `public/app.js` 里的状态对象
- 认证部分
- 博客模块
- 打卡模块
- 番茄钟模块

本周目标：

- 看懂当前用户是怎么保存在浏览器里的
- 看懂注册、登录、退出登录的流程
- 看懂博客、打卡、提醒是如何从界面触发接口的

建议你自己动手改 3 个功能：

- 给博客加一个“删除”按钮
- 给番茄钟加一个提示音开关
- 给打卡项加一个“备注”字段

## 第 3 周：学后端基础，并读懂 `server.js`

学习重点：

- Node.js 基础
- HTTP 请求与响应
- `GET` / `POST`
- JSON 数据处理
- 文件读写
- 接口路由

在项目里重点看：

- `server.js` 顶部配置
- 静态文件返回逻辑
- `/api/register`
- `/api/login`
- `/api/session`
- `/api/dashboard`

本周目标：

- 看懂浏览器请求是怎么到后端的
- 看懂后端是怎么校验参数的
- 看懂后端是怎么把数据写进 `store.json` 的

建议你自己动手改 2 个接口：

- 给博客增加删除接口
- 给打卡项增加编辑接口

## 第 4 周：把“数据”和“登录态”彻底学明白

学习重点：

- 会话 token 的作用
- 浏览器本地缓存与服务器数据的区别
- 为什么密码不能明文保存
- 为什么头像不能长期直接塞进 JSON

在项目里重点看：

- `public/app.js` 里的 `localStorage`
- `server.js` 里的密码哈希逻辑
- `data/store.json`

本周目标：

- 分清楚“本地缓存”和“服务器真实数据”
- 分清楚“用户数据”“会话数据”“业务数据”
- 明白现在这套方案为什么适合原型，不适合长期放大

建议你自己做 2 件事：

- 给 `store.json` 做一次备份
- 设计一版更合理的数据结构图

## 第 5 周：部署、排错、运维基础

学习重点：

- Linux 基础命令
- Node 服务启动与停止
- Nginx 反向代理
- HTTPS 证书
- 日志查看

结合你的环境重点学：

- 宝塔 Node 项目
- 宝塔网站反向代理
- SSL 申请

本周目标：

- 能独立把项目部署到服务器
- 能知道访问失败时去哪里看日志
- 能知道是前端错、后端错、反向代理错，还是权限错

建议你自己练习：

- 手动重启 Node 服务
- 修改一段页面文案后重新部署
- 故意改错一个端口，然后自己排查回来

## 第 6 周：把文件存储升级成数据库

学习重点：

- MySQL 基础
- 表设计
- 增删改查
- 用户表、博客表、打卡表、提醒表之间的关系

推荐升级方向：

- `users`
- `sessions`
- `blogs`
- `checkin_items`
- `checkin_completions`
- `notifications`

本周目标：

- 把 `data/store.json` 思维切换成数据库思维
- 能理解为什么 JSON 文件不适合用户量变大
- 为后面扩展功能打基础

建议你自己做的实战：

- 先画表结构
- 再把“用户”和“博客”迁到 MySQL
- 最后再迁“打卡”和“提醒”

## 第 7 周：把头像迁移到云存储

学习重点：

- 对象存储的概念
- 文件上传
- 存 URL，不存整张图片内容
- CDN 与访问速度

推荐方向：

- 阿里云 OSS

本周目标：

- 头像上传后进入对象存储
- 数据库或服务器只保存头像 URL
- 页面显示时直接加载云端图片

建议你自己做的实战：

- 新建 OSS Bucket
- 后端增加上传接口
- 用户头像从 base64 改为 URL

## 第 8 周：做正式上线前的优化

学习重点：

- 安全
- 备份
- 访问控制
- 基础性能优化
- 错误处理

需要逐步补上的点：

- 更严格的输入校验
- 限流
- 更规范的 session 过期处理
- 数据备份
- 日志记录
- 图片压缩策略
- 数据库备份

本周目标：

- 让这个项目从“能跑”进入“能长期维护”

## 每周固定学习方法

- 每周至少读 2 次代码，不少于 30 分钟
- 每周至少亲手改 1 个真实功能
- 每周至少自己排查 1 个 bug
- 每周写一份简短总结，记录你学会了什么、哪里还不会

## 推荐你的学习方式

不要这样学：

- 看很多课但不动手
- 一上来同时学 React、Vue、Nest、MySQL、Docker
- 一开始就追求“大厂标准架构”

建议这样学：

- 以这个项目为主线
- 每学一个知识点，立刻改项目
- 先会做，再慢慢做规范

## 你接下来最值得做的 6 个练习

1. 给博客加删除与编辑
2. 给打卡做日历视图
3. 给番茄钟加浏览器通知或提示音
4. 把 `store.json` 迁移到 MySQL
5. 把头像迁移到阿里云 OSS
6. 给网站加后台日志和数据备份

## 当前头像 / 照片存储现状

当前头像不是存在云端。

现在的真实存储方式是：

- 头像先在前端被处理成 `data:image/...;base64,...`
- 后端接收这个字符串
- 然后直接把整段 base64 图片数据写进 `data/store.json`

这意味着：

- 头像真实保存位置在服务器本地磁盘
- 不是阿里云 OSS、COS、S3 这种对象存储
- 不是数据库单独文件表
- 不是浏览器本地作为主存储

这种方式的优点：

- 简单
- 方便开发
- 原型阶段够用

这种方式的问题：

- `store.json` 会越来越大
- 头像越多，读写越慢
- 不适合正式长期运行
- 不方便做图片管理和 CDN 加速

## 你现在最合理的下一步

如果你的目标是“尽快上线”，建议顺序是：

1. 先把当前版本在服务器上稳定跑起来
2. 再把数据层从 JSON 升级到 MySQL
3. 然后把头像迁移到阿里云 OSS

不要一开始就同时改数据库、头像云存储、前端重构。那样容易全乱。

## 最后建议

你现在已经不是从零开始学了。你已经有一个完整的小项目。

真正应该做的是：

- 一边维护这个项目
- 一边补知识
- 一边把最薄弱的环节换成更正式的方案

这条路线会比“看一堆教程但没有项目落地”快得多。
